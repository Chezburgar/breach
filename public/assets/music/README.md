# Music

`deadzone.mp3` loops behind the menus. It fades in on the first click and ducks
out when a match starts, then comes back when you return to the menu.

To change the track, replace the file. The path is resolved relative to
`src/engine/audio.js`, so it works from a subdirectory on GitHub Pages as well
as from the root.
